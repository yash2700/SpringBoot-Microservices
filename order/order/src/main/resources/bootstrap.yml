server:
  port: 9005

spring:
  application:
    name: orderMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        defaultContext: application
        profileSeparator: "::"
        format: YAML