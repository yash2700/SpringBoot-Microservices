server:
  port: 9001

spring:
  application:
    name: accountMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        default-context: application
        profile-separator: '::'
        format: YAML