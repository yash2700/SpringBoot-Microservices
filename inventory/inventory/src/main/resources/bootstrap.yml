server:
  port: 9004

spring:
  application:
    name: inventoryMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        default-context: application
        profile-separator: '::'
        format: YAML